$("#list_room").append("<%= j render @rooms %>");
<% if @rooms.next_page %>
  $(".pagination").replaceWith("<%= j paginate @rooms, theme: "twitter-bootstrap-3" %>");
<% else %>
  $(window).off("scroll");
  $(".pagination").remove();
<% end %>
