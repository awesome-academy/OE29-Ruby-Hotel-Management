<% provide :title, t("global.all_service") %>
<div class="content-header">
  <div class="container-fluid">
    <button type="button" class="btn btn-primary float-sm-right" data-toggle="modal" data-target="#servicemodal">
      <i class="fas fa-plus"></i> <%= t ".add_service" %>
    </button>
    <div class="row">
      <div class="col-md-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title"><%= t ".list_service" %></h3>
          </div>
          <div class="card-body table-responsive p-0">
            <table class="table table-hover">
              <thead>
              <tr>
                <th><%= t ".id" %></th>
                <th><%= t ".service_name" %></th>
                <th><%= t ".price" %></th>
                <th><%= t ".des" %></th>
                <th><%= t ".unity" %></th>
                <th></th>
                <th></th>
              </tr>
              </thead>
              <tbody>
                <%= render @services %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<%= render "admin/services/form" %>
