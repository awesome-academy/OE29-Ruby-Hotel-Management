<div class="modal" id="unitymodal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-body">
        <%= form_for @unity, url: check_url_unity(@unity), remote: true do |f| %>
          <%= render "admin/shared/error_message", object: f.object %>
          <%= f.label :name %>
          <%= f.text_field :name, class: "form-control" %>
          <%= f.submit t(".submit"), class: "btn btn-primary mt-3"%>
        <% end %>
      </div>
    </div>
  </div>
</div>
